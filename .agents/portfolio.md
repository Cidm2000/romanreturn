# The portfolio, and the owner profile

<!-- GENERATED by agentos sync. A pointer, not a copy: both live once, in Agent OS. -->

This repository is one of several. Before framing substantial work here - especially
anything that would create new infrastructure - check whether the capability already
exists elsewhere, and whether this repository should own it at all.

```bash
node "$AGENT_OS_HOME/scripts/portfolio.mjs" context <this repo> "<the request>"
node "$AGENT_OS_HOME/scripts/portfolio.mjs" find "<capability>"
node "$AGENT_OS_HOME/scripts/portfolio.mjs" health
```

If `AGENT_OS_HOME` is not set, the portfolio layer is simply unavailable: say so and
proceed on this repository's own evidence. Never guess at what other repositories
contain, and never assume a capability is missing because you could not reach the
registry.

## Owner profile

Stable tiebreak preferences live once, privately, at `$AGENT_OS_HOME/owner/PROFILE.md`.
Read it when several reasonable approaches exist. It is **private**: never copy its
content into this repository, a generated artifact, a commit message, a log, or any
external service. It never overrides an explicit current instruction - see the
arbitration order in the managed block of `AGENTS.md`.

A project-specific preference that contradicts it belongs in this repository's own
rules, outside the managed block, and wins locally.
